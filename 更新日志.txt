========================================
Code Manager 更新日志
========================================

----------------------------------------
v10.4.0 (2026-05-13)
----------------------------------------
【性能优化】
• GradientButton：删除冗余第一遍渐变循环（原始代码绘制了两遍，第一遍完全被第二遍覆盖）
• GradientButton：新增颜色缓存（_cached_colors），colors 不变时复用已计算的渐变列表，
  大幅减少 hover 时的字符串解析开销

【Bug 修复】
• 修复 PlatformAPI 缺少 username 属性：Fork 后克隆时回退到 self.api.username 会
  AttributeError，补全初始化并在 get_user() 中赋值
• 修复 NetworkManager 连续限流后返回 None：3 次 429 后函数隐式 return None，
  改为保存最后一个响应并返回，避免调用方 .status_code 崩溃
• 修复 list_repos 三次重复解析 JSON：每次分页调用 r.json() 三次，改为缓存到 data

【封装改进】
• GitOps 新增 pull_unrelated(path, remote, branch)：封装 --allow-unrelated-histories 拉取
• GitOps 新增 pull_with_strategy(path, remote, branch, strategy)：封装带合并策略的拉取
• 消除 _do_upload 和 _do_commit_push 中直接调用私有 git._run(["pull",...]) 的封装泄漏

【代码质量】
• 合并 App.log 与 _log_to_ui：线程安全逻辑统一到 _log_to_ui，log() 只负责格式化后委托
• _get_theme 增加 _normalize_theme()：统一为各主题补全缺失的 fallback key
  （sidebar_bg/card_bg/tooltip_bg 等），消除散落在代码各处的 .get("key", default)
• NetworkManager 合并 Timeout + ConnectionError 重试逻辑，消除重复代码块
• 移除 _do_fork 内的重复 import time（顶层已导入）

【线程安全】
• _do_upload、_do_create_repo：desc/priv/lic/msg/gitignore 读取移至主线程
• _do_commit：ud_msg 读取移至主线程
• _do_push：ud_force 读取移至主线程
• _do_commit_push：ud_msg/ud_force 读取移至主线程
• _load_issues：is_state 读取移至主线程
• _load_pulls：pr_state 读取移至主线程

【安全】
• Token 加密本地存储：使用机器绑定的 XOR+base64 加密方案（_get_machine_key），
  配置文件中不再明文保存 Token，重启后自动解密恢复，无需重新输入
• 退出时只清内存（_cleanup 不再覆写磁盘），加密 Token 在下次启动时可自动还原
• 修复 EXE 启动崩溃：Token 迁移写入异常不再阻断启动，改为 try/except 静默跳过

----------------------------------------
v10.3.0 (2026-05-12)
----------------------------------------
【Bug 修复】
• 修复批量上传崩溃：替换不存在的 filedialog.askdirectories，改为循环单选目录
• 修复设置页面崩溃：新建/删除配置时对 StringVar 调用 .config() 导致 AttributeError，
  改为保存 Combobox 实例引用 st_profile_cb
• 修复分支列表/Fork 列表线程安全问题：后台线程直接操作 Tkinter 控件，
  改为 root.after(0, update) 确保在主线程更新 UI
• 修复提交推送双重提交：_do_commit_push 在 pull 前后各提交一次，
  整合为 pull → add → 单次 commit → push
• 修复日志轮转逻辑：.1 槽位从未创建，补充 rename(log_file → log_file.1)
• 修复 Issue/PR/Release 创建中 Text 控件跨线程读取：将 .get() 调用移至线程外捕获
• 修复 Fork 后立即克隆竞态：GitHub 202 表示异步创建，现等待仓库就绪（最多 30s）再克隆
• 修复 _repos_load 冗余表达式

【安全加固】
• export_config 导出时自动剥离 token_* 字段，防止凭据随配置文件泄露
• _cleanup（atexit）在退出时将 token 删除写回磁盘，防止凭据残留
• 推送失败不再静默强制推送，改为提示用户手动勾选"强制推送"选项

【代码质量】
• import re 从函数内部移至模块顶层
• 裸 except: 改为具体异常类型（json.JSONDecodeError / ValueError / TypeError）
• build_nuitka.bat 清除 git merge 冲突残留，更新至 v10.3 参数
• build_nuitka.bat 依赖改为 nuitka[onefile]，启用 zstandard 压缩

【构建优化】
• 安装 zstandard 后 onefile 体积从 40MB 压缩至 12MB（压缩率 70.6%）
• 修正废弃参数 --windows-disable-console → --windows-console-mode=disable

----------------------------------------
v10.2.0 (2026-05-11)
----------------------------------------
【UI调整】
• 操作日志面板移回右侧竖向显示

----------------------------------------
v10.1.0 (2026-05-11)
----------------------------------------
【Bug修复】
• 修复侧边栏Logo图标被截断问题，移除Logo图片仅保留文字
• 修复深色模式不完整问题，现在切换深色/浅色模式时全局UI完全重建
• 修复浅色模式下部分文字残留深色模式白色的问题

----------------------------------------
v10.0.0 (2026-05-11)
----------------------------------------
【全新UI设计】
• 全新Modern扁平化UI设计（Vercel/Notion风格）
• 侧边栏导航替代顶部标签页（Emoji图标 + 选中指示器 + hover效果）
• 卡片式容器布局（圆角边框 + 阴影效果）
• 渐变色按钮组件（Canvas绘制 + hover变亮）
• 自定义美化提示框（ToolTip组件）
• 新增Modern主题配色（蓝紫渐变，支持深色/浅色）

----------------------------------------
v9.6.0 (2026-05-08)
----------------------------------------
【Bug修复】
• 修复新建配置时 simpledialog 未导入导致的崩溃
• 统一所有字体为 Segoe UI，UI风格一致

【跨平台支持】
• 新增 GitHub Actions 自动构建 macOS 版本
• 新增 macOS 本地构建脚本 build_macos.sh

----------------------------------------
v9.5.0 (2026-05-08)
----------------------------------------
【UI美化】
• 采用 Segoe UI 字体，更现代的视觉效果
• 优化深色主题配色（GitHub Dark 风格）
• 增加控件间距和内边距，提升可读性
• Treeview 行高增加，更易点击
• 窗口默认大小调整为 1280x860

----------------------------------------
v9.4.0 (2026-05-08)
----------------------------------------
【Bug修复】
• 修复本地分支 master 与远程分支 main 不匹配问题
• 初始化时自动将分支重命名为 main
• 处理合并冲突

----------------------------------------
v9.3.0 (2026-05-08)
----------------------------------------
【Bug修复】
• 修复推送失败问题
• 推送前先拉取远程仓库内容

----------------------------------------
v9.2.0 (2026-05-08)
----------------------------------------
【新增功能】
• 初始化Git按钮
• 关联远程仓库按钮
• 初始化并关联（一键）按钮

----------------------------------------
v9.1.0 (2026-05-08)
----------------------------------------
【Bug修复】
• 修复更新推送时未检查Git仓库的问题

----------------------------------------
v9.0.0 (2026-05-07)
----------------------------------------
【新增功能】
• Issue管理、PR管理、Webhook配置
• 仓库搜索、统计面板、设置页面
• 批量操作、.gitignore模板选择

----------------------------------------
v8.0.0 (2026-05-07)
----------------------------------------
【安全加固】
• 使用Nuitka编译，源码保护大幅提升
• 反编译难度从⭐提升到⭐⭐⭐⭐
• 编译为机器码，运行速度略有提升

----------------------------------------
v7.1.0 (2026-05-07)
----------------------------------------
【Release功能优化】
• 创建Release前先验证仓库是否存在
• 详细错误提示（404/422等）

----------------------------------------
v7.0.0 (2026-05-07)
----------------------------------------
【用户体验优化】
• 添加加载状态指示
• 危险操作确认对话框
• 输入格式验证

----------------------------------------
v6.0.0
----------------------------------------
• 初始版本
• 支持GitHub/Gitee双平台
• 上传代码、更新推送、分支管理、Fork仓库

========================================
